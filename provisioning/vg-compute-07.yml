---
- name: "deploy jenkins localhost "
  gather_facts: false
  hosts: localhost   
  become: yes
  # roles:
  #   - common
  #   - jenkins
  tasks:
  # - name: Ensure apache is at the latest version
  #   ansible.builtin.yum:
  #     name: httpd
  #     state: latest    
  - name: "OS fingerprinting "
    debug:
      msg:
      - "First message"

# - include_tasks: prepare.yml